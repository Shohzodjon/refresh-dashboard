import { FC } from "react";

import styles from "./custom_modal_open_button.module.scss";

const CustomModalOpenButton: FC = () => {
  return (
    <div className={styles.div}>
      <div></div>
    </div>
  );
};

export default CustomModalOpenButton;
